{
  "name": "root",
  "private": true,
  "version": "independent",
  "scripts": {
    "web": "lerna exec --scope web-component yarn start",
    "mobile": "lerna exec --scope mobile-component yarn dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "dependencies": {
    "@types/node": "^14.14.41",
    "typescript": "^4.2.4"
  }
}
